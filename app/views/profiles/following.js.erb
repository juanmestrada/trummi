$('.fol-bloc-results').append('<%= j render(@profile_following) %>');
<% if @profile_following.next_page %>
	$('.pagination-more').removeClass('disabled');
	$('.pagination-more').attr('href', '<%= following_profile_path(page: @profile_following.next_page) %>');
	$('.pagination-more').html("<i class='fas fa-plus-circle'></i> <span>More</span>");
<% else %>
	$('.pagination-more').remove();
<% end %>